// Header is now integrated into the sidebar - this component is kept for backward compatibility
// but renders nothing. The branding and navigation are handled by the sidebar.
export function Header() {
  return null
}
